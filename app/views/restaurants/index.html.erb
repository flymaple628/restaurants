	<div class="well">
		<div class="row">
			this is a test col.
		</div>
	</div>
	<div class="new">
		<div class="row">
		<% if params[:id].nil? %>
		<%=form_for @restaurant ,:url=>restaurants_path do |f|%>
			<%= render :partial=>'form',:locals=>{ :f=>f} %>
		<% end %>
		<% else %>
			<%=form_for @restaurant ,:url=>restaurant_path(@restaurant),:method=>:patch do |f|%>
			<%= render :partial=>'form',:locals=>{ :f=>f} %>
			<% end %>
		<% end %>
		</div>
		<div class="row">

		</div>
	</div>
<div class="row">
	<div class="col-md-12"><%= link_to "new",new_restaurant_path,:class=>"btn btn-default" %>
	</div>
</div>
<table class="table table-striped">
	<thead >
		<tr>
			<th class="col-md-2">名字</th>
			<th class="col-md-6">位置</th>
			<th class="col-md-4"></th>
		</tr>
	</thead><!--thead-->
	<tbody class="tbody">
		<% @restaurants.each do |f| %>
		<tr >
			<td class="col-md-2"><%=f.name %></td>
			<td class="col-md-6"><%=f.location%></td>
			<td class="col-md-4">
				<div class="btn-group">
						<%= link_to "show", restaurant_path(f),:class=>"btn btn-primary" %>
						<%#= link_to "edit", edit_restaurant_path(f),:class=>"btn btn-default" %>
						<%= link_to "edit", edit_restaurant_path(f,:page=>params[:page]),:class=>"btn btn-default" %>
						<%= link_to "delete", restaurant_path(f),:method=>:delete,:class=>"btn btn-danger" %>
				</div><!--btn-group-->
			</td>
		</tr>
	<% end %>
	</tbody><!--tbody-->
</table><!--table-->
<div class="row">
	<div class="col-md-12 text-center"><%= paginate @restaurants %></div>
</div>